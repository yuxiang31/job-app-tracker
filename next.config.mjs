/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  outputFileTracingRoot: import.meta.dirname,
  devIndicators: false,
};

export default nextConfig;
